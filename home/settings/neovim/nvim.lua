-- ========================================================================== --
-- ==                              ENTRY POINT                             == --
-- ========================================================================== --
-- Порядок важен:
-- 1. options   — vim.opt настройки, не зависящие от плагинов
-- 2. keymaps   — общие маппинги (без кастомизации базовых motion/buffer команд)
-- 3. autocmds  — автокоманды
-- 4. lazy      — bootstrap менеджера плагинов + загрузка спеков из lua/plugins/*
--
-- Плагин-специфичные keymaps/setup живут рядом с самим плагином в lua/plugins/,
-- а не здесь — так проще найти, что относится к чему.

require('config.options')
require('config.keymaps')
require('config.autocmds')
require('config.lazy')
