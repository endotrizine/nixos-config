-- Оба плагина ниже НЕ добавляют новых сочетаний и НЕ переопределяют
-- ванильные vim-команды. Они тренируют эффективное использование уже
-- существующих ванильных motion (10j вместо jjjjjjjjjj, счётчики, zz, %,
-- text objects и т.п.) — то есть работают в твою пользу: чем лучше ты
-- знаешь ванильный vim, тем спокойнее заходить на голый сервер.

return {
  {
    'm4xshen/hardtime.nvim',
    dependencies = { 'MunifTanjim/nui.nvim', 'nvim-lua/plenary.nvim' },
    event = 'VeryLazy',
    opts = {
      -- по умолчанию ловит злоупотребление hjkl/повторами без счётчика
      -- и подсказывает эффективную ванильную альтернативу (не блокирует
      -- команды как таковые — просто не даёт закрепиться неэффективной привычке)
    },
  },

  -- :VimBeGood — открывает отдельную игру-тренажёр motion по команде,
  -- никак не влияет на обычное редактирование, пока явно не запущена
  { 'ThePrimeagen/vim-be-good', cmd = 'VimBeGood' },
}
