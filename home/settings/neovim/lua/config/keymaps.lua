-- ========================================================================== --
-- ==                          GENERAL KEYMAPS                             == --
-- ========================================================================== --
-- Осознанно НЕ трогаем: hjkl, dd/yy/p, ciw/caw, gg/G, поиск /, undo u,
-- сплиты <C-w>, и любые другие ванильные vim-биндинги.
-- Все маппинги ниже — это либо команды по умолчанию отсутствующие в vanilla vim
-- (typo-фиксы, quit-out shortcuts для help/man), либо явно завязаны на leader,
-- так что на сервере без этого конфига просто не сработают ':q' — и это нормально,
-- ты не будешь ожидать что они там есть.

-- Toggle absolute/relative line numbers в command-mode делает конфиг чуть удобнее
-- для копирования номеров строк, но не трогает никакую motion-логику — см. autocmds.lua

-- Быстрый reload конфига (полезно во время правки)
vim.api.nvim_create_user_command('ReloadConfig', 'source $MYVIMRC', {})
